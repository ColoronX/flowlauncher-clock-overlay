<Window x:Class="FlowLauncher.RealTimeClock.ClockOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Clock Overlay"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        MouseRightButtonDown="Window_MouseRightButtonDown">

    <Border x:Name="RootBorder" CornerRadius="8" Background="#CC1E1E1E" Padding="12,6">
        <Border.Effect>
            <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0" Color="Black"/>
        </Border.Effect>
        <TextBlock x:Name="TimeText"
                   Foreground="White"
                   FontSize="28"
                   FontWeight="Bold"
                   FontFamily="Segoe UI"
                   HorizontalAlignment="Center"/>
    </Border>
</Window>
